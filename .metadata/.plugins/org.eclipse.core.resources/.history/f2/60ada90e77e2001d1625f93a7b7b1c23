package com.sangoc.mapper;

import java.sql.ResultSet;
import java.sql.SQLException;

import com.sangoc.model.AbstractModel;

public class AbstractMapper implements GenericMapper<AbstractModel> {
	@Override
	public AbstractModel mapRow2(ResultSet resultset) {
		try {
			AbstractModel abstractModel = new AbstractModel();
			abstractModel.setCreatedDate(resultset.getTimestamp("createdDate"));
			abstractModel.setCreatedBy(resultset.getString("createdBy"));
			abstractModel.setModifiedBy(resultset.getString("modifiedBy"));
			abstractModel.setModifiedDate(resultset.getTimestamp("modifiedDate"));
			return abstractModel;
		} catch (SQLException e) {
			return null;
		}
	}
}
